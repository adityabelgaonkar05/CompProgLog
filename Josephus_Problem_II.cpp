#include <bits/stdc++.h>
#define ab adityabelgaonkar
using namespace std;

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    vector<int> s;
    int n;
    cin >> n;
    int k;
    cin >> k;

    for (int i = 1; i <= n; ++i)
        s.push_back(i);

    vector<bool> vis(n);

    return 0;
}