#include <bits/stdc++.h>
#define int long long
#define ab adityabelgaonkar
using namespace std;

int32_t main()
{
    int t; cin >> t;
    while(t--)
    {
        int n; cin >> n;
        vector<string> s(2);
        cin >> s[0] >> s[1];
        string ans;

        int updown = 0;
        int count = 0;
        for(int i = 0; i < n-1; ++i)
        {
            
        }

        cout << ans << "\n";
        cout << max(count, 1ll) << "\n";
    }

    return 0;
}