#include <bits/stdc++.h>
#define int long long
#define endl '\n'
#define ab adityabelgaonkar
using namespace std;

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        string s;
        cin >> s;

        int finalv;
        if (s[0] == '1')
            finalv = 1;
        else
            finalv = 0;

        for (int i = 1; i < n; ++i)
        {
            if (!(i & 1))
            {
                        }
        }
    }

    return 0;
}