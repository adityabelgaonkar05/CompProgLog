#include<bits/stdc++.h>
#define int long long
#define ab adityabelgaonkar
using namespace std;

int32_t main()
{
    int t; cin >> t;
    while(t--)
    {
        int p1, p2, p3; cin >> p1 >> p2 >> p3;
        if(p1 == p2 && p2 == p3 && (p1&1)) cout << -1 << "\n";
        else
        {
            
        }
    }

    return 0;
}