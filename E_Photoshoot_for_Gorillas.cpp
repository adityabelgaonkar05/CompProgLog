#include <bits/stdc++.h>
#define int long long
#define endl '\n'
#define ab adityabelgaonkar
using namespace std;

void traversal(vector<vector<int>> &b, vector<int> &a)
{
}

int32_t main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--)
    {
        int n, m, k, w;
        cin >> n >> m >> k;

        vector<int> a(w);

        for (auto &i : a)
            cin >> i;

        sort(a.begin(), a.end());

        vector<vector<int>> b(n, vector<int>(m, 0));
    }

    return 0;
}