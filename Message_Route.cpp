#include <bits/stdc++.h>
#define int long long
#define ab adityabelgaonkar
using namespace std;

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // bfs simple

    int n, m;
    cin >> n >> m;

    int dist = INT_MAX;

    vector<vector<int>> a(n + 1);

    while (m--)
    {
        }

    return 0;
}