#include<bits/stdc++.h>
#define int long long
#define ab adityabelgaonkar
using namespace std;

int32_t main()
{
    int t; cin >> t;
    while(t--)
    {
        int k, q; cin >> k >> q;
        vector<int> a(k);
        for(auto &i : a) cin >> i;
        vector<int> b(q);
        for(auto &i : a) cin >> i;
        
    }

    return 0;
}